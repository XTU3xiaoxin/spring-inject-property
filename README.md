spring-inject-property
======================

version 1.0

the function list:

1. a flexible method that override the default value through spring property injection
2. the example that inject static method for a bean in spring
